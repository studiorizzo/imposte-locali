import { Colors } from '../../theme';

export function AliquotePage() {
  return (
    <div
      className="h-full flex items-center justify-center"
      style={{ color: Colors.greyWeak }}
    >
      Aliquote (coming soon)
    </div>
  );
}
